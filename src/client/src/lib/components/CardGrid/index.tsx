import React from 'react';
import { Grid, Card } from '@mui/material';

export const CardGrid = () => {
  return (
    <Grid container>
      <Grid item spacing={3} sm={2}>
        <Card>
          
        </Card>
      </Grid>
    </Grid>
  )
}